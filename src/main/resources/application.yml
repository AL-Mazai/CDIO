# 端口号
server:
  port: 8083

# 配置数据库
spring:
  datasource:
    url: jdbc:mysql://:3306/cdio_pro1?serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: root
    password: zzwzzw
    driver-class-name: com.mysql.cj.jdbc.Driver

# 配置 MyBatis
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.cdiopro.pojo
  configuration:
    map-underscore-to-camel-case: true
